# Maiplot web (admin dashboard) — SCRUM-59 / SCRUM-60
# Base URLs of the backend services, called server-side (token stays server-side).
AUTH_SERVICE_URL=http://localhost:8011
LISTING_SERVICE_URL=http://localhost:8012
REALTOR_SERVICE_URL=http://localhost:8017
NOTIFICATION_SERVICE_URL=http://localhost:8016
ANALYTICS_SERVICE_URL=http://localhost:8018

# Web Push (SCRUM-121). Base64url VAPID PUBLIC key, exposed to the browser as the
# PushManager applicationServerKey. Must match VAPID_PUBLIC_KEY on
# notification-service. Empty (dev/fake-push default) disables the in-browser
# opt-in. NEXT_PUBLIC_ prefix = readable client-side (public by design).
NEXT_PUBLIC_VAPID_PUBLIC_KEY=

# Comma-separated IP allowlist for the /admin surface. Empty = allow any
# (local/dev default). Production sets the office/VPN egress IPs; Kong enforces
# the same list at the edge — this app-level check is defence in depth.
ADMIN_IP_ALLOWLIST=
